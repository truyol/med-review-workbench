# 验收矩阵

文档状态：Approved for P1  
更新时间：2026-09-20

| 需求 | 用户可观察结果 | 自动验证 | 演示证据 |
|---|---|---|---|
| FR-001 | 可以创建项目和脱敏病例 `DEMO-TAVR-001` | API 集成测试；数据库测试 | 病例出现在项目详情页 |
| FR-002 | 可以把 DICOM、STL、图片归入同一病例 | 上传/关联接口测试 | 病例下出现两个合成 PNG、`CT_small_anonymized.dcm`、`0002_anonymized.DCM`、`aorta.stl` |
| FR-003 | 素材展示 UUID、大小、哈希和格式 | 文件服务单元测试 | 素材卡片显示基础信息 |
| FR-004 | 素材卡片显示来源、标签、状态、备注 | API/前端组件测试 | 修改状态后列表立即更新 |
| FR-005 | DICOM 显示白名单元数据、缩略图和帧数 | pydicom 单元测试；隐私字段扫描 | `CT_small_anonymized.dcm` 显示 128x128；`0002_anonymized.DCM` 显示 96 帧 |
| FR-006 | STL 可旋转、缩放、平移、复位和添加结构标记 | 前端组件测试；Playwright 主流程 | 操作 `aorta.stl` 或 `LA.stl` |
| FR-007 | 评审结论可保存，刷新后仍存在 | 数据库集成测试 | 保存“可用/需补充”结论并刷新 |
| FR-008 | 可按类型、状态和标签筛选素材 | 前端组件测试；API 查询测试 | 切换三个筛选维度后列表变化正确 |
| FR-009 | 图片可浏览、选择两张并排比较，并通过标签、状态和备注整理 | 前端组件测试；Playwright 主流程 | 对照 `synthetic-cardiac-ct-baseline.png` 与 `synthetic-cardiac-ct-annotated.png`，保存整理结果和评审结论 |
| FR-010 | 缺失/损坏/不支持/保存失败显示错误码、下一步和 `request_id` | 异常接口测试；Playwright 异常流 | 上传损坏样例或模拟保存失败 |
| FR-011 | 日志可关联请求且不含敏感字段 | 日志扫描测试 | 展示一条带 `request_id` 的请求链路 |
| NFR-002 | SQLite 可本地保存数据，文档说明 PostgreSQL 切换路径 | 迁移测试 | 重启后结论仍在 |
| NFR-006 | 只展示 DICOM 白名单字段 | 单元测试；快照测试 | 页面不展示 PatientName/PatientID |

## 验收原则

- 必须有明确输入、预期结果和证据位置。
- “页面看起来完成”不视为通过。
- Must 需求优先形成一条完整闭环；Should 需求不阻塞主流程交付。
- 涉及 DICOM、日志和文件名的验收必须包含隐私检查。
